server.port: 8081

spring:
  application.name: fraud
  datasource:
    password: sasa
    url: jdbc:postgresql://localhost:5432/fraud
    username: postgres
  jpa:
    hibernate.ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true

  zipkin:
    base-url: http://localhost:9411

